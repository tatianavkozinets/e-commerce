import React from "react";
import Title from "../Title";

const EmptyCart = () => {
  return (
    <div className="container mt-5">
      <Title name="You " title="cart is empty" />
    </div>
  );
};

export default EmptyCart;
